---
description: Autonomous Implementation of Perkfinity Onboarding & Auth
---

// turbo-all

1. Implement the "Privacy-First" Onboarding screen in `app/onboarding/page.tsx` using glassmorphism and modern animations.
2. Build the Multi-Method Auth interface in `app/auth/page.tsx` (Apple, Google, Email).
3. Create the PII collection form (Name, Email, Phone, City, Zip) with responsive design.
4. Implement the Permission Request logic for Geolocation and Notifications.
5. Update the main `page.tsx` navigation to link to these new flows.
6. Verify the build locally and document results in `walkthrough.md`.
