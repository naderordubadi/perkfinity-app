node_modules/
.next/
out/
build/
# Environment variables — ALL .env files ignored. NEVER commit secrets.
# Use Vercel/CI environment variables for deployed environments.
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Firebase config — never commit these
GoogleService-Info.plist
google-services.json
# Large media files — never commit to git
*.MP4
*.mp4
*.mov
*.MOV
